import React from 'react'

const Button = ({name}) => {
  return (
    <div>
         <button style={{
            padding: "4px 7px",

            }}>{name}</button>
    </div>
  )
}

export default Button
